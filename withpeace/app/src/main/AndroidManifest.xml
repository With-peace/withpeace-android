<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.with_peace.withpeace">

    <application
        android:name=".global.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Withpeace">
        <activity
            android:name=".feature.main.MainActivity"
            android:exported="true">

        </activity>
        <activity android:name=".feature.signIn.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.signIn.SignInAdminActivity">

        </activity>
    </application>

</manifest>